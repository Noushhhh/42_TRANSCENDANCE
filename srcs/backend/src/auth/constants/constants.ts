export const jwtConstants = {
    secret: process.env.JWT_SECRET,
  };

// ─────────────────────────────────────────────────────────────────────────────
//defaul avatar
export const DEFAULT_AVATAR_PATH = 'uploads/defaultProfileImage.jpg';