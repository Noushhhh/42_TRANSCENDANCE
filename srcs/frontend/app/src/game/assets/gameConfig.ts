export interface GameConfig {
  konvaWidth: number;
  konvaHeight: number;
  paddleHeight: number;
  paddleWidth: number;
}

export const gameConfig: GameConfig = {
  konvaWidth: 1200,
  konvaHeight: 800,
  paddleHeight: 150,
  paddleWidth: 25,
};
