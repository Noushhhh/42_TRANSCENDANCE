import React from 'react';
import '../styles/loadingSpinner/LoadingSpinner.css'; // Import the CSS file

const LoadingSpinner: React.FC = () => {
  return <div className="spinner"></div>;
};

export default LoadingSpinner;
